import React from 'react';
import PhotoUpload from './components/PhotoUpload';

const App: React.FC = () => {
    return (
        <div>
            <h1>Recycle Photo App</h1>
            <PhotoUpload />
        </div>
    );
};

export default App;